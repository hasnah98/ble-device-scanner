# ble-device-scanner
A simple Python application that scans nearby Bluetooth Low Energy (BLE) devices using the Bleak library. The scanner detects devices in real time, filters out duplicates, and displays each device’s name and MAC address in a clean, readable format.
